import { bool, int, nat, float32, Principal, text } from 'azle';

// Boolean
const myBoolean: bool = true;

// Integer
const positiveInt: int = 50n;
const negativeInt: int = -100n;

// Natural
const natural: nat = 100n;

// float32
const positiveFloat32: float32 = 0.5;
const negativeFloat32: float32 = -0.5;

// text
const myText: text = 'hello';

// Principal
const myPrincipal: Principal = Principal.fromText('rwlgt-iiaaa-aaaaa-aaaaa-cai');
